<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Màu nền bên trong (nếu cần) -->
    <solid android:color="#FFFFFF"/>

    <!-- Cài đặt viền -->
    <stroke
        android:width="1dp"
        android:color="@color/black"
        /> <!-- Màu viền đỏ -->

    <!-- Bo góc (tùy chọn) -->
    <corners android:radius="5sp"/>
</shape>